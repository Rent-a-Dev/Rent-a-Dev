<link rel="stylesheet" href="css/Main.css" type="text/css">
<header>
  <%if(showNav) {%>
  <details id="logoutDetail">
    <summary><%=username%></summary>
    <form action="/logout" method="GET">
      <button type="submit">Log-out</button>
    </form>
  </details>
  <%}%>
  <h1 class="MainHeading">
    Rent-a-Dev
  </h1>
  <%if(showNav) {%>
    <nav>
      <a <% if (title === "View Devs") {%> class="currentLink" <%}%> href="/viewDevs">View available devs</a>
      <a <% if (title === "Team requests") {%> class="currentLink" <%}%> href="/viewRequests">View my requests' status</a>
      <a <% if (title === "Approve requests") {%> class="currentLink" <%}%> href="/approveRequests">Manage my team requests</a>
      <a <% if (title === "Manage Devs") {%> class="currentLink" <%}%> href="/manageDevs">Manage my devs</a>
    </nav>
    <%}%>
  </header>
